#include<stdio.h>
// printing size of int , char , float , double type variable
int main()
{
    int a;
    char b;
    float c;
    double d;
    printf("size of a=%d , size of b=%d , size of c=%d , size of d=%d\n",sizeof(a),sizeof(b),sizeof(c),sizeof(d));
    return 0;
}